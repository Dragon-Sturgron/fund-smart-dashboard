{
  "name": "edgeone-fund-dashboard",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "基金行情与规则化买卖辅助面板，适配 EdgeOne Makers，并支持 KV 云端保存。",
  "scripts": {
    "check": "node --check public/assets/app.js && node --check edge-functions/api/fund/[code].js && node --check edge-functions/api/config.js",
    "serve": "python -m http.server 4173 -d public"
  }
}
