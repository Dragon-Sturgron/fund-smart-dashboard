# 基智盘 v2.5.1 份额修正版

本版基于 v2.5.1 连续新增版，修复持有份额使用盘中估算净值导致的偏差。

## 修复

- 持有份额改为：`持有金额 ÷ 上一确认净值`。
- 不再使用不断变化的盘中估算净值计算固定份额。
- 对旧版已经按盘中估算净值计算的持仓，首次进入设置页会自动按上一确认净值修正一次。
- 保留基金名称自动识别和“添加并继续”。
- KV 仍只保存基金代码。

## 更新

```bash
git add -A
git commit -m "修复持有份额计算净值来源"
git push origin main
```
